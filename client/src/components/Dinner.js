import React from 'react';
  
    export default function dinner(props) {
        return (
            <button className="
            
            " onClick={props.onClick}>show</button>
         
        )
      }