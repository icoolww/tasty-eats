import React from 'react';
  
    export default function lunch(props) {
        return (
            <button className="
            
            " onClick={props.onClick}>show</button>
         
        )
      }